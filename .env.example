# Optional server-side fallback. Users can also enter keys in the app
# under Settings → API keys (stored only on the device).
# If unset, analysis still runs from SEC filings and market data.

# OPENAI_API_KEY=
# GROQ_API_KEY=
# OPENROUTER_API_KEY=
# ANTHROPIC_API_KEY=
# AI_MODEL=
# ANTHROPIC_MODEL=
# PORT=3001
